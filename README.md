# -Sentinel-A-Distributed-Asset-Connectivity-Monitor
A system that periodically checks the health and latency of thousands of targets (URLs, APIs, or IP addresses) using a controlled worker pool to ensure it doesn't overwhelm the network or local CPU.
